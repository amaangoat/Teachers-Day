# Teachers' Day Arcade

Mobile-first celebration game with a shared live leaderboard.

Run `node server.js`, then open `http://localhost:3000`. For the carnival, deploy on any Node 24+-compatible host with persistent storage for `data/teachers-day.sqlite`.
